{
  "name": "autoimprove",
  "version": "2.0.0",
  "description": "Language-agnostic autonomous codebase improvement loop. Detects your stack, builds a scoring suite, then iterates: propose → implement → measure → keep/discard. Works with any language or framework.",
  "author": "autoimprove",
  "skills": ["detect-stack", "improve-loop", "measure", "rollback"],
  "commands": ["improve", "measure", "status", "setup"]
}
